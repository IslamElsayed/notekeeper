<%= react_component("Note", props: { note: @note.as_json(include: [:tags]), note_users: @note_users, roles: @roles, users: @users, current_note_user: @note.note_user_for(current_user), authenticity_token: form_authenticity_token, notice: notice  }) %>

<% if policy(@note).edit? %>
  <%= link_to 'Edit', edit_note_path(@note) %> |
<% end %>
<%= link_to 'Back', notes_path %>
