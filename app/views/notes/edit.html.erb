<h1>Editing Note</h1>

<%= react_component("NoteForm", props: { note: @note.as_json(include: [:tags]), errors: @note.errors.full_messages, authenticity_token: form_authenticity_token, tags: Tag.all }) %>

<%= link_to 'Show', @note %> |
<%= link_to 'Back', notes_path %>
